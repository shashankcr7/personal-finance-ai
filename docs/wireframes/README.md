# Wireframes

Export the five frames from Figma as PNGs into this folder:
`login.png`, `upload.png`, `dashboard.png`, `dashboard-trends.png`, `goals.png`, `chat.png`.

Figma: select frame → Export → PNG (2x). `../UI_SPEC.md` is the authoritative
spec for components and states; these images show layout intent.
